# Content

All page copy, the weekly log, literature list, PDF links, and contact details live in **`index.html`** at the project root.

There is no separate content module. Edit the HTML directly, then run `npm run build` before deploying `dist/`.
